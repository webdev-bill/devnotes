// Shared classNames for form fields across Login, NoteForm, and BlogForm —
// kept in one place once a third form needed the exact same treatment.
export const inputClass =
  'mt-1 block w-full rounded-md border border-rule bg-white px-3 py-2 font-body text-sm text-ink shadow-sm focus:border-keyword focus:outline-none'

export const labelClass = 'font-display text-xs tracking-wide text-ink/60 uppercase'
